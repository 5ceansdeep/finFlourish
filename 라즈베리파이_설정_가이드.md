# 🍓 라즈베리파이 설정 가이드

Fin & Flourish 앱과 연동하기 위한 라즈베리파이 설정 방법입니다.

---

## 📋 목차
1. [필수 패키지 설치](#1-필수-패키지-설치)
2. [FastAPI 서버 파일 만들기](#2-fastapi-서버-파일-만들기)
3. [서버 실행](#3-서버-실행)
4. [IP 주소 확인](#4-ip-주소-확인)
5. [연결 테스트](#5-연결-테스트)
6. [자동 실행 설정 (선택)](#6-자동-실행-설정-선택)

---

## 1. 필수 패키지 설치

라즈베리파이 터미널에서 순서대로 실행:

```bash
# 시스템 업데이트
sudo apt update
sudo apt upgrade -y

# Python pip 설치
sudo apt install python3-pip -y

# FastAPI와 Uvicorn 설치
pip3 install fastapi uvicorn[standard]

# 설치 확인
python3 -c "import fastapi; import uvicorn; print('설치 완료!')"
```

---

## 2. FastAPI 서버 파일 만들기

### 2-1. 프로젝트 폴더 생성

```bash
cd ~
mkdir rpi_api
cd rpi_api
```

### 2-2. main.py 파일 작성

```bash
nano main.py
```

아래 코드를 복사해서 붙여넣기:

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
import random

app = FastAPI()

# CORS 설정 (React Native 앱 접근 허용)
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.get("/")
def read_root():
    return {"message": "Fin & Flourish API Server Running!"}

@app.get("/api/sensors")
def get_sensor_data():
    """
    센서 데이터 반환
    TODO: 실제 센서 값으로 교체 필요
    """
    return {
        "tds": random.randint(30, 80),
        "temp": round(22 + random.random() * 6, 1),
        "ph": round(6.5 + random.random() * 1.5, 1),
        "status": random.choice(["happy", "worry", "angry"])
    }

@app.post("/api/feed")
def feed_fish():
    """
    먹이 주기 명령
    TODO: GPIO 핀 제어 코드 추가
    """
    print("🐟 먹이 주기 명령 수신!")
    # 예: GPIO.output(FEED_PIN, GPIO.HIGH)
    return {"success": True, "message": "먹이 주기 완료"}

@app.get("/temperature")
def read_temp():
    """테스트용 엔드포인트"""
    temp = round(25 + random.random() * 5, 2)
    return {"temp": temp}
```

**저장 방법:**
- `Ctrl + O` (저장)
- `Enter` (파일명 확인)
- `Ctrl + X` (종료)

---

## 3. 서버 실행

### 3-1. 기본 실행 (테스트용)

```bash
cd ~/rpi_api
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

**성공 메시지:**
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
```

> ⚠️ 이 방법은 터미널을 닫으면 서버도 종료됩니다.

### 3-2. 백그라운드 실행 (계속 실행)

```bash
cd ~/rpi_api
nohup uvicorn main:app --host 0.0.0.0 --port 8000 > server.log 2>&1 &
```

**실행 중인지 확인:**
```bash
ps aux | grep uvicorn
```

**서버 종료 방법:**
```bash
pkill -f uvicorn
```

**로그 확인:**
```bash
tail -f ~/rpi_api/server.log
```

---

## 4. IP 주소 확인

라즈베리파이의 IP 주소를 확인합니다:

```bash
hostname -I
```

**출력 예시:**
```
192.168.0.42
```

> 📝 이 IP 주소를 메모하세요! React Native 앱 설정에 사용됩니다.

---

## 5. 연결 테스트

### 5-1. 라즈베리파이에서 테스트

```bash
# 기본 엔드포인트
curl http://localhost:8000

# 센서 데이터
curl http://localhost:8000/api/sensors

# 먹이 주기
curl -X POST http://localhost:8000/api/feed
```

### 5-2. 같은 와이파이의 다른 기기에서 테스트

컴퓨터나 스마트폰 브라우저에서 접속:

```
http://172.20.10.2:8000
http://172.20.10.2:8000/api/sensors
http://172.20.10.2:8000/docs

**예상 응답:**
```json
{
  "tds": 65,
  "temp": 25.3,
  "ph": 7.2,
  "status": "happy"
}
```

---

## 6. 자동 실행 설정 (선택)

라즈베리파이 부팅 시 자동으로 서버 실행:

### 6-1. systemd 서비스 파일 생성

```bash
sudo nano /etc/systemd/system/finflourish.service
```

아래 내용 입력:

```ini
[Unit]
Description=Fin & Flourish API Server
After=network.target

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/rpi_api
ExecStart=/usr/local/bin/uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### 6-2. 서비스 활성화

```bash
# 서비스 활성화
sudo systemctl enable finflourish.service

# 서비스 시작
sudo systemctl start finflourish.service

# 상태 확인
sudo systemctl status finflourish.service
```

### 6-3. 서비스 관리 명령어

```bash
# 시작
sudo systemctl start finflourish

# 중지
sudo systemctl stop finflourish

# 재시작
sudo systemctl restart finflourish

# 상태 확인
sudo systemctl status finflourish

# 로그 확인
sudo journalctl -u finflourish -f
```

---

## ✅ 체크리스트

라즈베리파이 설정 완료 확인:

- [ ] Python3 및 pip 설치 완료
- [ ] FastAPI, Uvicorn 설치 완료
- [ ] `~/rpi_api/main.py` 파일 생성 완료
- [ ] 서버 실행 성공 (http://localhost:8000 접속 가능)
- [ ] IP 주소 확인 완료 (`hostname -I`)
- [ ] 같은 와이파이의 다른 기기에서 접속 테스트 완료
- [ ] (선택) 자동 실행 설정 완료

---

## 🔧 문제 해결

### 문제 1: 포트 8000이 이미 사용 중

```bash
# 8000번 포트 사용 중인 프로세스 확인
sudo lsof -i :8000

# 프로세스 종료
sudo kill -9 [PID]
```

### 문제 2: 외부에서 접속 안 됨

```bash
# 방화벽 설정 (필요 시)
sudo ufw allow 8000
sudo ufw status
```

### 문제 3: 모듈을 찾을 수 없음 (ModuleNotFoundError)

```bash
# pip 경로 확인
python3 -m pip show fastapi

# pip로 다시 설치
python3 -m pip install fastapi uvicorn[standard]
```

---

## 📱 다음 단계: React Native 앱 설정

라즈베리파이 설정이 완료되면, React Native 앱에서 IP 주소를 변경하세요:

**파일:** `services/sensorService.ts`

```typescript
const API_BASE_URL = 'http://192.168.0.42:8000';  // 실제 라즈베리파이 IP
```

그 다음:
```bash
npm start
```

---

## 🎯 실제 센서 연동 (다음 단계)

현재는 랜덤 데이터를 반환합니다. 실제 센서와 연동하려면:

1. **TDS 센서** - 수질 오염도 측정
2. **온도 센서** - DS18B20 등
3. **pH 센서** - pH 측정 모듈
4. **먹이 급여 장치** - GPIO 핀으로 모터 제어

센서 라이브러리 설치 예시:
```bash
pip3 install adafruit-circuitpython-ads1x15
pip3 install RPi.GPIO
```

---

## 📞 도움말

- FastAPI 공식 문서: https://fastapi.tiangolo.com/ko/
- 라즈베리파이 GPIO: https://www.raspberrypi.com/documentation/computers/gpio.html
